interface:
  display_name: "Multi-Session Development"
  short_description: "Lead가 Claude·Codex 세션 lane을 worktree별로 병렬 실행하고 통합"
  default_prompt: "Use $multi-session-dev to split this task into independent lanes, run them as isolated sessions, and integrate the results."
policy:
  allow_implicit_invocation: true
