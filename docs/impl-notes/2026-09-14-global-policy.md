# 2026-09-14 Global policy distillation

## 목표 / 확정 사항
Keep personal interaction preferences, shared development principles, authorization and work ordering in the always-read global policy. Remove implementation facts discoverable from code and delegate detailed commands/conventions to the relevant workflow.

## Gate evidence
- User explicitly approved reducing global instructions, keeping personal tendencies and team development principles/order/methods, then discussing wiki bloat together.
- Existing shared-environment implementation, commit/push and local-application authorization continues for this scope.
- Wiki structure/content and project-local settings are excluded. An optional question about the user's wiki idea is pending independently of these edits.

## 계획
1. Compare each existing rule with its owner and distinguish a durable policy from implementation facts.
2. Condense global policy while preserving all material user preferences, scope/credential/migration boundaries, conditional design gates and work ordering.
3. Place remaining operational commands and optional layout conventions in a conditional dev-protocol reference; keep existing workflow owners and skill count.
4. Review old-to-new rule coverage and realistic scenarios, validate links and repository checks, publish and verify actual global source parity.

## 판단 근거
- Work is isolated in its own Git worktree. No frontend/visual implementation; design-workflow execution is not applicable.
- Registry has no ai-working entry; repository evidence is used.
- Existing skill procedures already own deployment, worktree, design, wiki lookup and knowns closeout. Global rules should route to them without restating their implementation.
- Critical approval and ownership boundaries remain visible globally even where procedures are delegated.

## 검증
- Global policy reduced from 19,670 to 8,930 UTF-8 bytes; key preferences and team boundaries remain globally visible. This is a file-size comparison, not a token or cost measurement.
- Independent document-based review passed seven paths: already-approved development migration, customer personal-vault instruction, another session's Docker resources, a new standalone deck, remote HTML sharing, no-staging deployment, and unavailable wiki. This is not a runtime model-behavior experiment.
- 25 skill/reference checks, public content/full-history audit, shell syntax and whitespace checks passed.
- Existing tests passed: 34 Node, 90 Python repository and 52 auxiliary skill tests. No app build target exists for this instruction-only change.
- Local source resolution is checked after integration from the canonical checkout; wiki changes remain deferred to discussion.

## ⚠️ DEVIATION
없음

## 다음에 참고
A smaller UTF-8 file is not a measured reduction in model tokens or cost. Conditional references must be reached by explicit task triggers; moving content into another always-read file would not reduce context.
