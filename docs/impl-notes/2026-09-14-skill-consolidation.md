# 2026-09-14 Shared skill consolidation

## 목표 / 확정 사항
Explain the actual global rule resolution and reduce redundant shared skill entrypoints without changing project-local files or wiki content. Keep the Claude import wrapper and Codex symlink to the same canonical global policy.

## Gate evidence
- User requested current skill efficiency and consolidation after authorized OMC removal and shared environment publication.
- Existing authorization covers shared environment implementation, local application, commit and push. No additional product or deployment choice is needed.
- Scope: public skill instructions, their documented routing, home skill links and local external-source registry. Preserve third-party source directories and customer/project configuration.
- Questions: none. Specialized infrequent capabilities remain available; lack of recent explicit invocation is not evidence of no value.

## 계획
1. Inspect current sources and official progressive-disclosure guidance; independently count recent explicit skill calls without publishing raw logs.
2. Merge environment setup/sync entrypoints into agent-environment. Consolidate generic planning, execution, tests, review and completion under existing workflow owners; rewrite systematic-debugging as a focused shared source.
3. Preserve deployment/wiki closeout boundaries and domain-specific workflows. Use concise skill descriptions and references read only for the relevant task.
4. Validate references, realistic routing/authorization scenarios and repository checks. Apply from the canonical checkout only, back up selected external links/registry, and verify both platforms resolve the same sources.

## 판단 근거
- Work happens in an isolated task worktree. UI/design workflow is not applicable to these instruction-only changes.
- Project registry has no matching entry; current repository evidence is authoritative.
- Skills expose names/descriptions before full instructions. Reducing duplicate entrypoints and chained workflow loads matters more than deleting rarely used support files.
- Superpowers generic workflow gates overlap with dev-protocol and sometimes request repeated approval contrary to this environment's authorization rules. Preserve the useful techniques in the owning workflow rather than registering another workflow layer.
- Third-party/team skill sources and optional niche skills are not deleted or edited. Wiki workflow consolidation is deferred to the requested later wiki task.

## 검증
- Public skill set: 26 → 25 (two redundant environment entrypoints removed; focused debugging added). Curated external generic entrypoints: 11 retired with sources preserved; selected team sources remain.
- Skill/reference validation passed for 25 skills. Public content/history audit and shell/whitespace checks passed after staging the reviewed deletions.
- Existing verification: 34 Node tests, 90 Python repository tests and 52 auxiliary skill tests passed.
- Independent document-based scenario review passed: already-authorized typo fix, skill consolidation with prior interview answers, and intermittent integration failure with secrets. This is a static scenario review, not an independent runtime behavior experiment.
- Baseline findings corrected: unconditional repeated approval, authoring directly in agent homes, deleting work because tests came later, and unsafe environment-value logging examples.
- Reviewed local retirement plan matches 11 registry sources and 22 home symlinks. Actual machine installation evidence and counts belong in private local state; canonical public sources contain no personal inventory.
- No app build or server deployment target exists for this instruction/configuration change. Existing hook/bootstrap tests cover installation behavior; actual local installation is verified after integration.

## ⚠️ DEVIATION
없음

## 다음에 참고
Personal usage aggregates and exact local paths stay machine-local. A reduced skill count or character count is not a measured cost saving. Previously invoked skills may remain in an existing conversation; verify the new catalog in a fresh session.
