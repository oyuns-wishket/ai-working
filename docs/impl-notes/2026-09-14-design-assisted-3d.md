# 2026-09-14 Design workflow: motion, charts, and assisted 3D

## 목표 / 확정 사항
- Make Motion design part of web UI work across ERP, commerce, and consumer services; reuse approved patterns and preview new choices.
- Use Anime.js for requested animation work and Bklit UI for compatible chart work, with local installation/reuse and actual integration.
- Carry a 3D beginner through asset sourcing/modeling, visual iteration, export, and verified web integration.
- Select capabilities per provider and current free/paid entitlement; an existing subscription does not imply API access, every export, or unlimited credits.

## Gate evidence
- 사용자 요청·답변: The reviewed plan was accepted with novice-friendly AI collaboration and free/paid operation as explicit refinements.
- 확인 질문: No unresolved choice is required to author the reusable workflow. Account and project capabilities are discovered when the workflow runs.
- 승인된 범위: Update the shared design skill and its supporting references/templates; verify and apply the shared source. The continuing skill work retains the earlier commit/push publication authorization.
- 미승인 가정: No live modeling service, purchase, account login, asset upload, or customer web deployment is performed by this maintenance task.
- This is skill maintenance, not a visual deliverable; frontend composition gates apply to future design tasks, not these instruction edits.

## 계획
1. Run independent baseline scenarios against the existing skill.
2. Update routing, local app setup, interactive preview rules, and reusable decision fields.
3. Add capability-aware provider selection and an AI-led 3D creation-to-web reference.
4. Independently forward-test realistic free/paid and mixed-product scenarios.
5. Run repository validators, affected tests and public publication checks; apply canonical changes and verify runtime links.

## 판단 근거
- Baseline: a free 3D novice receives generic modeling candidates without a concrete agent-owned creation/export loop; a paid Spline self-host request has no entitlement-to-delivery path.
- Baseline: existing guidance selects CSS and possibly Recharts for mixed commerce/consumer work; Motion and Bklit are optional, contrary to the accepted defaults.
- Keep provider guidance progressive and capability based; do not hardcode prices, credit quantities, account identifiers, or personal plans in the public skill.
- Existing app-install instructions are sufficient; the Taste/Impeccable helper retains its narrow role. No speculative credential or subscription integration is added.
- Repository has no matching knowledge registry entry; use repository evidence. This is a workflow repository, not an ERP runtime despite keyword-based scanner signals.

## 검증
- Before change: design-tool installer regression suite, 11 tests passed.
- Independent baseline: three scenarios exposed the missing routing and collaboration instructions above.
- After change: independent forward simulations passed four 3D cases (free beginner, limited Spline entitlement, web/API billing separation, depleted credits) and five UI cases (mixed product tasks, installed tool reuse, Vue compatibility, read-only audit, included Motion+ access).
- Independent staged-diff review found no blocking conflict. Clarified multi-screen mode selection and the audit exception at the verification reference entry after review.
- Python suites: 142 tests passed across the repository and skill-local suites. Node suites: 34 tests passed.
- Skill validation, skill-creator validation, public audit including history, shell syntax, JSON parsing, and whitespace checks passed. No application build applies to this instruction-only repository change.
- Publication and canonical runtime link checks are completed during release; actual model generation, provider access, and browser rendering are future project acceptance checks, not claims of this maintenance verification.

## ⚠️ DEVIATION
- 없음

## 다음에 참고
- Rendering libraries do not supply product fidelity or morph-ready assets.
- Free generation, downloadable output, commercial rights, API access, and fully independent hosting are separate capabilities.
- Keep the user responsible for visual intent and unavoidable account actions; the agent owns technical production and web integration.
